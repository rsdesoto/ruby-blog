<h1>Blog Pages</h1>

<!-- <%= @pages.first.title %> -->

<!--
    <% 4.times do %>
        <p>Fish</p>
        <p><%= @pages.first.title %></p>
    <% end %>
-->

<% @pages.each do |page| %>
    <p><%= link_to page.title, page_path(page) %></p>
    <!--<p><%= page.title %></p>-->
<% end %>

<%= link_to "New Page", new_page_path %>